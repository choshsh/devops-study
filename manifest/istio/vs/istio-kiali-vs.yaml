apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-kiali-route
  namespace: istio-system
spec:
  hosts:
    - "istio-kiali.choshsh.com"
  gateways:
    - default/choshsh-gateway
  http:
    - name: "kiali-http"
      match:
        - port: 80
      route:
        - destination:
            host: kiali
            subset: v2
            port:
              number: 20001
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: kiali
  namespace: istio-system
spec:
  host: kiali
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
  subsets:
    - name: v2
      labels:
        version: v2
      trafficPolicy:
        loadBalancer:
          simple: ROUND_ROBIN
nameOverride: "kibana"
fullnameOverride: "kibana"

deploy:
  labels:
    version: "v1"

service:
  type: NodePort
  port: 5601

elasticsearch:
  host: "http://elasticsearch"
  port: "9200"
  username: "elastic"
  password: "elastic"

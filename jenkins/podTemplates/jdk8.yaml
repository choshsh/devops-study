spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node-role.kubernetes.io/jenkins
                operator: Exists
  tolerations:
    - key: "node-role.kubernetes.io/jenkins"
      operator: "Exists"
      effect: "NoSchedule"
  containers:
    - name: jdk
      image: amazoncorretto:8
      imagePullPolicy: IfNotPresent
      command:
        - sleep
      args:
        - 99d
      volumeMounts:
        - name: gradle-caches
          mountPath: /mnt/.gradle-caches
    - name: git
      image: alpine/git:latest
      imagePullPolicy: IfNotPresent
      command:
        - sleep
      args:
        - 99d
  volumes:
    - name: gradle-caches
      secret:
        secretName: gradle-caches
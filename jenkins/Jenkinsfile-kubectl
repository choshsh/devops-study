pipeline {
  agent {
    kubernetes {
      yamlFile 'jenkins/pod_template/kubectl.yaml'
    }
  }
  stages {
    stage('Test') {
      steps {
        container('kubectl') {
            sh 'kubectl get po -A'
        }
      }
    }
  }
}
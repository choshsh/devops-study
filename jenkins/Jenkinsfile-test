pipeline {
  agent {
    kubernetes {
      yamlFile 'jenkins/pod_template/test.yaml'
    }
  }
  stages {
    stage('Test') {
      steps {
        container('ubuntu') {
          sh 'hostname'
        }
      }
    }
  }
}